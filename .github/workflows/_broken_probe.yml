name: intentionally broken
on: [push]
jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ steps.nonexistent.outputs.nope }}"
        if: ${{ github.event.this_context_does_not_exist }}
